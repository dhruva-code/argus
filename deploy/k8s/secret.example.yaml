# TEMPLATE ONLY — do not commit real values.
# Create the real Secret with e.g.:
#   kubectl -n argus create secret generic argus-secrets \
#     --from-literal=POSTGRES_PASSWORD=... \
#     --from-literal=DATABASE_URL='postgresql+asyncpg://argus:...@postgres:5432/argus' \
#     --from-literal=JWT_SECRET=$(openssl rand -hex 32) \
#     --from-literal=SECRET_ENCRYPTION_KEY=$(python -c 'from cryptography.fernet import Fernet;print(Fernet.generate_key().decode())') \
#     --from-literal=ORCH_INTERNAL_TOKEN=$(openssl rand -hex 16) \
#     --from-literal=S3_ACCESS_KEY=argus --from-literal=S3_SECRET_KEY=...
apiVersion: v1
kind: Secret
metadata:
  name: argus-secrets
  namespace: argus
type: Opaque
stringData:
  POSTGRES_PASSWORD: CHANGE_ME
  DATABASE_URL: postgresql+asyncpg://argus:CHANGE_ME@postgres:5432/argus
  JWT_SECRET: CHANGE_ME
  SECRET_ENCRYPTION_KEY: CHANGE_ME
  ORCH_INTERNAL_TOKEN: CHANGE_ME
  S3_ACCESS_KEY: argus
  S3_SECRET_KEY: CHANGE_ME
